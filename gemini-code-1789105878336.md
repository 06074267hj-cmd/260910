# 프로그램 결과보고서 작성도우미 — 빌드 프롬프트

> 아래 전체를 복사해서 Claude(또는 다른 코딩 AI)에 그대로 붙여넣으면 됩니다.
> `【 】` 표시된 부분만 본인 기관 상황에 맞게 고쳐 쓰세요.

---

## [역할]

너는 단일 HTML 파일로 동작하는 실무 도구를 만드는 프론트엔드 개발자다.
정신건강복지센터 실무자가 **프로그램 결과보고서**를 작성할 때, 회기 기록과 척도 점수만 입력하면 기관 서식 그대로 초안이 완성되는 웹 도구를 만들어라.

## [절대 제약 — 반드시 지킬 것]

1. **로그인·회원가입을 만들지 않는다.** 사용자 계정, 인증, 권한 개념이 전혀 없다.
2. **서버·데이터베이스를 쓰지 않는다.** 백엔드 API 호출, fetch, 외부 통신이 0건이어야 한다. 인터넷 없이 열어도 완전히 동작해야 한다.
3. **개인정보를 저장하지 않는다.** `localStorage`, `sessionStorage`, `IndexedDB`, 쿠키를 일절 사용하지 않는다. 입력값은 메모리(자바스크립트 변수)에만 존재하며 새로고침하면 사라진다. 이 사실을 화면 하단에 항상 보이도록 명시한다.
4. **파일 하나(HTML)로 동작한다.** CSS와 JS를 모두 인라인으로 넣고, 외부 라이브러리·외부 폰트·CDN을 쓰지 않는다. `.html` 파일 하나를 더블클릭하면 바로 열려야 한다.
5. 저장이 필요하면 **사용자가 직접 파일로 내려받는 방식**만 허용한다. (작업 내용을 `.json`으로 다운로드 → 나중에 그 파일을 다시 불러오기) 브라우저 안에는 아무것도 남기지 않는다.
6. UI는 한국어, 인쇄는 A4 세로 기준으로 맞춘다.

---

## [1. 사용자가 입력해야 하는 부분]

입력 패널을 아래 5개 섹션(접기/펼치기)으로 구성한다.

### ① 기본정보

| 입력 항목 | 형식 | 비고 |
|---|---|---|
| 프로그램명 | 한 줄 텍스트 | 필수 |
| 기간 시작일 / 종료일 | 날짜 | 회기 기록에서 자동 채움 가능 |
| 총 회기수 | 자동 계산 | 회기 표의 행 개수로 자동 산출 |
| 장소 | 한 줄 텍스트 | 예: 센터 집단상담실 |
| 강사 | 한 줄 텍스트 | 이름 + 직함 |
| 대상 | 한 줄 텍스트 | 회원 표기 + 나이/성별/진단명 |
| 작성자 | 한 줄 텍스트 | 필수 |

**대상 입력칸 옆에 상시 안내 문구:** "실명 대신 이니셜(예: 이○○) 사용을 권장합니다."

### ② 목적 · 목표

- **목적**: 여러 줄 텍스트 1~2문장. (프로그램을 왜 했는지)
- **목표**: 항목 추가 버튼으로 여러 개 입력. 출력 시 `1)`, `2)`, `3)` 자동 번호.

### ③ 회기 기록 (핵심 입력)

표 형태로 행 추가/복제/삭제/순서 이동이 가능해야 한다.

| 열 | 형식 | 비고 |
|---|---|---|
| 회기 | 자동 번호 | 행 순서에 따라 자동 |
| 날짜 | 날짜 | |
| 회기 중 측정 점수 | 숫자(비워둘 수 있음) | 미실시 회기는 공란 → 출력 시 `-` |
| 진행 내용 | 여러 줄 텍스트 | 실무자가 메모하듯 자유롭게 |
| 특이사항 | 체크박스 | `중단` / `일정변경` / `방식변경` 등 태그 |

- 마지막 행에서 Enter를 누르면 다음 행이 자동 추가되도록 한다.
- 날짜 기준 자동 정렬 버튼을 둔다.

### ④ 척도 정보

**척도 추가 시 아래 5개 프리셋 중에서 고르게 한다.** 프리셋을 고르면 방향·총점범위·해석 주의사항이 자동으로 채워지고, 사용자가 수정할 수 있다. `직접 입력` 옵션도 둔다.

| 프리셋 | 유형 | 방향 | 회기 중 반복측정 |
|---|---|---|---|
| MHS:DR (정신건강 선별도구: 우울) | 사전-사후 비교형 | 낮을수록 좋음 | 가능(주력) |
| PSS (지각된 스트레스 척도) | 사전-사후 비교형 | 낮을수록 좋음 | 보통 안 함 |
| GOL | 사전-사후 비교형 | **높을수록 좋음** | 보통 안 함 |
| 한국형회복척도 (K-RSA, 회복자기평가척도) | 사전-사후 비교형 | **높을수록 좋음** | 보통 안 함 |
| 만족도 설문 (5점 척도) | 사후 단일값형 | 높을수록 좋음 | 해당 없음 |

척도 하나마다 카드 형태로 아래를 입력받는다.

| 입력 항목 | 형식 | 비고 |
|---|---|---|
| 척도명 | 프리셋 선택 또는 직접 입력 | |
| 유형 | 자동 채움 | `사전-사후 비교형` / `사후 단일값형` |
| **점수 방향** | 자동 채움 (수정 가능, 필수) | `낮을수록 좋음` / `높을수록 좋음` |
| **총점 범위(최소~최대)** | 숫자 2개 (필수) | 변화폭을 비율로 환산하는 데 사용 |
| 사전 점수 / 사후 점수 | 숫자 | |
| 보고 방식 | 선택 | `총점` / `문항 평균` |
| 회기 점수 연동 | 체크 | 체크 시 ③의 점수 열을 이 척도의 회기별 추이로 사용 |

**점수 방향과 총점 범위는 해석 문장을 통째로 뒤집는 값이므로, 미설정 상태에서는 초안 생성을 막고 경고를 띄운다.**

### ⑤ 진행자 관찰 메모 (문장화 재료)

아래 항목을 여러 줄 텍스트로 받는다. 완성된 문장이 아니라 메모여도 된다.

- 참여 태도 · 이해도 · 집중력
- 진행 중 조정한 것 (설명 반복, 기록방식 변경 등)
- 중단·지연이 있었다면 그 사유
- 종결 시점의 대상자 상태 / 종결 사유
- 만족도 조사 자유응답 (있는 경우 원문 그대로)
- 향후 계획

---

## [2. 출력값 — 이 서식 그대로 만들 것]

미리보기 영역에 아래 구조의 표를 **실제 문서 모양 그대로** 렌더링한다.

### 2-1. 머리표 (2열 표)

```text
프로그램 | 【프로그램명】
일  시   | 【시작일 ~ 종료일, 총 N회기】
장  소   | 【장소】            강 사 | 【강사】
대  상   | 【대상】
작성자   | 【작성자】